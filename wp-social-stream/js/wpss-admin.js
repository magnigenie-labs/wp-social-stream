jQuery(function($){
	$("#background").wpColorPicker({
		defaultColor: "#333"
	}),
	$("#text_color").wpColorPicker({
		defaultColor: "#fff"
	}),
	$("#link_color").wpColorPicker({
		defaultColor: "#f6dd97"
	}),
	$("#link_hover").wpColorPicker({
		defaultColor: "#fff5d8"
	});
});